---
interface Props {
  decorative?: boolean;
  alt?: string;
  className?: string;
}

const {
  decorative = true,
  alt = "Shortcut Fox warning accent",
  className = ""
} = Astro.props;
---
<img
  class:list={["mascot-accent", "mascot-fox", className]}
  src="/assets/mascots/shortcut-fox-warning.svg"
  alt={decorative ? "" : alt}
  aria-hidden={decorative ? "true" : undefined}
  width="96"
  height="96"
  loading="lazy"
  decoding="async"
/>
